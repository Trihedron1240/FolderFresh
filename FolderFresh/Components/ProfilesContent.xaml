<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="FolderFresh.Components.ProfilesContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FolderFresh.Components"
    xmlns:models="using:FolderFresh.Models"
    Loaded="UserControl_Loaded">

    <Grid Background="#1C1C1C">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <Grid Grid.Row="0" Padding="24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock Text="Profiles"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="White"
                           VerticalAlignment="Center"/>
                <TextBlock Text="Save and switch between different organization configurations"
                           FontSize="13"
                           Foreground="#666666"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <!-- Import Button -->
                <Button x:Name="ImportButton"
                        Click="ImportButton_Click"
                        Background="#2D2D2D"
                        Foreground="#B5BAC1"
                        Padding="12,8"
                        CornerRadius="6"
                        ToolTipService.ToolTip="Import a profile from file">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B5;" FontSize="14"/>
                        <TextBlock Text="Import" FontSize="13"/>
                    </StackPanel>
                </Button>

                <!-- New Profile Button -->
                <Button x:Name="NewProfileButton"
                        Click="NewProfileButton_Click"
                        Background="#5865F2"
                        Foreground="White"
                        Padding="12,8"
                        CornerRadius="6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="12"/>
                        <TextBlock Text="New Profile" FontSize="13" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Info Banner -->
        <Border Grid.Row="1"
                x:Name="InfoBanner"
                Background="#2D2D2D"
                CornerRadius="8"
                Padding="12"
                Margin="24,0,24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Info Icon -->
                <FontIcon Grid.Column="0"
                          Glyph="&#xE946;"
                          FontSize="16"
                          Foreground="#5865F2"
                          VerticalAlignment="Center"
                          Margin="0,0,12,0"/>

                <!-- Info Text -->
                <TextBlock Grid.Column="1"
                           Text="Each profile contains its own rules, categories, and settings. Switch profiles to use different organization configurations for different purposes."
                           FontSize="13"
                           Foreground="#999999"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"/>

                <!-- Dismiss Button -->
                <Button Grid.Column="2"
                        x:Name="DismissBannerButton"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="8"
                        Click="DismissBannerButton_Click">
                    <FontIcon Glyph="&#xE711;" FontSize="12" Foreground="#666666"/>
                </Button>
            </Grid>
        </Border>

        <!-- Profiles List -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Padding="24,0,24,24">
            <StackPanel x:Name="ProfilesListPanel" Spacing="12"/>
        </ScrollViewer>
    </Grid>
</UserControl>
